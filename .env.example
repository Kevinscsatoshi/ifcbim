# CAD2BIM Studio 环境变量示例
# 复制为 .env 后按需修改（Docker Compose 会自动读取 .env）

# 转换任务存储目录（默认：项目内 data/jobs 或临时目录）
# CAD2BIM_JOB_DIR=/app/data/jobs

# ODA File Converter 可执行文件路径（仅当未在 PATH 中时需要）
# ODA_FILE_CONVERTER_PATH=/usr/bin/ODAFileConverter

# 全线上部署时不要设置 VERCEL，否则会禁用 DWG 转换
# VERCEL=1
